<% ids.each do |elementId| %>
  <%= tinymce(selector: "##{elementId}") %>
<% end %>

<style type="text/css">
  /* Reset TinyMCE menu styles */
  .mce-tinymce button {
    font-size: inherit !important;
    text-transform: inherit !important;
  }
  .mce-tinymce button:hover {
    background-color: inherit !important;
  }
</style>
